// define variables for click
var patcount = 0;

// click kitty and see how many times you click
function add() {
    patcount = patcount + 1
    document.getElementById('text').value = patcount;
}

function save() {
    // will be adding a way to save progress here once I learn more about storage
}

function load() {
    // will be adding a way to load a save file
}

